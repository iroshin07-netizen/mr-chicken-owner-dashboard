import {useState} from "react"; import {Store,UserRound,LogOut,ShieldCheck} from "lucide-react"; import {useAuth} from "../context/AuthContext";
export default function Settings(){const {signOut}=useAuth();const [owner,setOwner]=useState("Irshad");const [restaurant,setRestaurant]=useState("Mr. Chicken");return <div className="max-w-3xl space-y-5"><div><h2 className="section-title text-2xl">Settings</h2><p className="muted mt-1">Keep your owner profile and restaurant identity tidy.</p></div><div className="card p-6 space-y-5"><div className="setting-row"><div className="icon-box red"><UserRound size={19}/></div><div className="flex-1"><p className="font-medium">Owner name</p><p className="text-xs text-gray-500 mt-1">Shown in your owner console.</p></div><input className="input max-w-xs" value={owner} onChange={e=>setOwner(e.target.value)}/></div><div className="setting-row"><div className="icon-box gold"><Store size={19}/></div><div className="flex-1"><p className="font-medium">Restaurant name</p><p className="text-xs text-gray-500 mt-1">Customer-facing brand name.</p></div><input className="input max-w-xs" value={restaurant} onChange={e=>setRestaurant(e.target.value)}/></div><div className="setting-row"><div className="icon-box green"><ShieldCheck size={19}/></div><div className="flex-1"><p className="font-medium">Realtime protection</p><p className="text-xs text-gray-500 mt-1">Supabase Auth + RLS should protect owner data.</p></div><span className="status delivered">Enabled</span></div></div><button className="btn danger-btn" onClick={signOut}><LogOut size={18}/> Log out</button></div>}